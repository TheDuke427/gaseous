# Use the official Blinko image published on Docker Hub
FROM blinkospace/blinko:latest

# If the upstream image already exposes and runs the server on 1111,
# no extra commands are required. Otherwise you can override entrypoint/cmd.
# Keep this image minimal — the container's CMD should start the app.
